import React from "react";
import TakePasswordContainer from "../../containers/TakePassword/TakePasswordContainer";
import DefaultTemplate from "../../template/DefaultTemplate";

const TakePasswordPage = () => {
  return (
    <DefaultTemplate>
      <TakePasswordContainer />
    </DefaultTemplate>
  );
};

export default TakePasswordPage;
